package com.brave.authority.service;

import com.brave.authority.entity.AcRole;
import com.brave.authority.pojo.Role;

import java.util.List;

public interface RoleService extends  BaseService<AcRole,Integer> {
}
